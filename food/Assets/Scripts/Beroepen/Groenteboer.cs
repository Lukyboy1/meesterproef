using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Groenteboer : MonoBehaviour
{
    public bool IsGroenteboer;
    // Start is called before the first frame update
    void Start()
    {
        IsGroenteboer = true;
    }
}
