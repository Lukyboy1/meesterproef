using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Veeboer : MonoBehaviour
{
    public bool IsVeeboer;
    // Start is called before the first frame update
    void Start()
    {
        IsVeeboer = true;
    }
}
