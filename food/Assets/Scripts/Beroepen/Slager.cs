using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Slager : MonoBehaviour
{
    public bool IsSlager;
    // Start is called before the first frame update
    void Start()
    {
        IsSlager = true;
    }
}
